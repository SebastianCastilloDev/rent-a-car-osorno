# ============================================
# CONFIGURACIÓN DE ENTORNO - RENT-A-CAR
# ============================================
# Copia este archivo a .env y ajusta los valores según tu entorno
# cp .env.example .env

# Ambiente de ejecución
NODE_ENV=development

# Puerto del servidor
PORT=3000

# ============================================
# CONFIGURACIÓN DE BASE DE DATOS
# ============================================

# Opción 1: Usar DATABASE_URL (para producción en Render, etc.)
# DATABASE_URL=postgresql://usuario:password@host:5432/nombre_db

# Opción 2: Variables individuales (desarrollo local con Docker)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=rentacar_db

# ============================================
# CONFIGURACIÓN JWT (AUTENTICACIÓN)
# ============================================

# Secret para firmar tokens JWT (¡CAMBIA ESTO EN PRODUCCIÓN!)
JWT_SECRET=tu-secreto-jwt-super-seguro-cambiar-en-produccion
JWT_EXPIRES_IN=24h

# ============================================
# CONFIGURACIÓN CORS
# ============================================

# Origen permitido para CORS (frontend)
CORS_ORIGIN=http://localhost:3001

# ============================================
# CONFIGURACIÓN RATE LIMITING
# ============================================

# Tiempo en milisegundos para la ventana de tiempo (default: 60000 = 1 minuto)
THROTTLE_TTL=60000

# Número máximo de requests por ventana de tiempo (default: 10)
THROTTLE_LIMIT=10
